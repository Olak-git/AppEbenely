import { HOST, PRODUCTION } from "../constants/data"

const ref_public = PRODUCTION ? "https://ebenely.project.utechaway.com/" : `${HOST}/projects/ebenely` // http://www.wylbee.company/superwoman/

export default ref_public